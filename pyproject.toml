[project]
name = "caishui-data-pipeline"
version = "0.1.0"
description = "财税知识库数据清洗微服务（解析 → 分块 → 元数据 → 核验 → 向量入库）"
requires-python = ">=3.11,<3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"
